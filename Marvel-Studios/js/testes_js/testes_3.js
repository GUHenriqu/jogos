let limite = 100;
let saldo_atual = 10;

let resultado = (saldo_atual < limite) ? "nescessito depositar $" ; "nao nescessito deposito";

alert(resultado)

/* 
sintexe
let varialvel = (condição) ? //instrução se true; // instrução se false
*/
